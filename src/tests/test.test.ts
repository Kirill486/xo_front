import {equal} from 'assert';

describe('base arithmetics', () => {
  it('Can calculate simple result', () => {
    const simpleArithmeticResult = 2 + 3;
    equal(simpleArithmeticResult, 5);  
  });
})
